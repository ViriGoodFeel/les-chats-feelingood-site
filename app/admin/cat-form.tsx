import type { Cat } from "@/lib/types";

export function CatForm({ cat, action, submitLabel }: { cat?: Cat; action: (formData: FormData) => void; submitLabel: string }) {
  const status = cat?.status || "brouillon";
  return <form className="panel stack" action={action}><div className="form-grid"><label className="field">Nom<input name="name" defaultValue={cat?.name} required /></label><label className="field">Âge<input name="age" defaultValue={cat?.age} placeholder="Ex : 2 ans" required /></label><label className="field">Sexe<select name="sex" defaultValue={cat?.sex || "Femelle"}><option>Femelle</option><option>Mâle</option><option>Inconnu</option></select></label><label className="field">Frais d'adoption<input name="adoption_fee" defaultValue={cat?.adoption_fee} placeholder="Ex : 180 €" required /></label><label className="field">Stérilisé<select name="sterilized" defaultValue={String(cat?.sterilized ?? true)}><option value="true">Oui</option><option value="false">Non</option></select></label><label className="field">Vacciné<select name="vaccinated" defaultValue={String(cat?.vaccinated ?? true)}><option value="true">Oui</option><option value="false">Non</option></select></label><label className="field full">Photos du téléphone ou de l'ordinateur<input name="photos" type="file" accept="image/*" multiple /></label>{cat?.photos?.length ? <div className="field full"><strong>Photos déjà enregistrées</strong><div className="photo-preview">{cat.photos.map((photo) => <img key={photo} src={photo} alt="Photo déjà enregistrée" />)}</div></div> : null}<label className="field full">État de santé<textarea name="health_condition" defaultValue={cat?.health_condition} required /></label><label className="field full">Personnalité<textarea name="personality" defaultValue={cat?.personality} required /></label><label className="field full">Histoire du sauvetage<textarea name="rescue_story" defaultValue={cat?.rescue_story} required /></label><label className="field full">Besoins particuliers<textarea name="special_needs" defaultValue={cat?.special_needs || "Aucun besoin particulier connu."} /></label><label className="field full">Publication<select name="status" defaultValue={status}><option value="brouillon">Garder en brouillon</option><option value="publie">Publier en un clic</option><option value="adopte">Marquer comme adopté</option></select></label></div><div className="actions"><button className="btn" type="submit">{submitLabel}</button><a className="btn secondary" href="/admin">Annuler</a></div></form>;
}
